[pytest]
markers =
    training: needs torch, and trains the model for real steps
# Unmarked tests are the fast ones and run everywhere. A new test file is fast
# until it says otherwise, so nothing silently drags torch into the quick job.
addopts = -m "not training"
